@AGENTS.md

## Agent skills

### Issue tracker

任务与规格存放在本仓库 `.scratch/`。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五个 triage 标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

使用单上下文布局：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
