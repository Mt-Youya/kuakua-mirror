# 06: 交互边界收尾与全量验收

**What to build:** 动画期间所有按键请求被忽略(不积压、不打断动画);连续短按 B 动画独占播放;长按 B(≥800ms)重连行为原样保留;动画结束回到干净待机。全量验收:串口起止各一次且间隔 ≈3.5s、帧率达标、文案轮换、LED 三相位、切回无花屏。

**Blocked by:** 03, 04, 05

**Status:** ready-for-agent

- [ ] 动画期间按 A/B,动画结束后无排队交互发生
- [ ] 动画中再按 B 不会让动画重入或连播
- [ ] 长按 B 触发重连,与动画互不干扰
- [ ] 全量验收清单逐项通过(对照 0002 spec 的 Testing Decisions)